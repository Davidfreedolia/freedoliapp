# Supabase
VITE_SUPABASE_URL=https://edjwsrkcxcktnbbskpjy.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# Google Drive OAuth
VITE_GOOGLE_CLIENT_ID=626952790156-2d8m9q2m6n9hbb8ll8ni9nasnc8levp4.apps.googleusercontent.com

# Feature flags (all default OFF in production)
# Enable tracking-sync UI (17TRACK backend is currently mock — keep OFF until real contract ready)
VITE_ENABLE_TRACKING_SYNC=false
# Beta override — bypass the seat-limit gate on the frontend.
# Backend (Edge Functions / RPC) still enforces; this is purely a UX unblock
# while we are in closed beta. Turn OFF before public launch.
VITE_DISABLE_SEAT_LIMIT=true
